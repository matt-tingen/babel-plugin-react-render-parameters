class Test extends PureComponent {
  render(another, yetAnother) {
    const props = this.props,
          state = this.state,
          context = this.context;

    return React.createElement('div', null);
  }
}
