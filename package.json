{
  "name": "babel-plugin-transform-react-render-parameters",
  "version": "0.3.0",
  "description": "Allow props and state as render() parameters, as in preact",
  "repository": "matt-tingen/babel-plugin-transform-react-render-parameters",
  "author": "Matt Tingen <matt.tingen@gmail.com>",
  "main": "lib/index.js",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.4.5",
    "babel-preset-es2015": "^6.3.13",
    "babel-preset-stage-0": "^6.3.13",
    "mocha": "^2.2.5"
  },
  "scripts": {
    "clean": "rm -rf lib",
    "build": "babel src -d lib",
    "test": "mocha --compilers js:babel-register",
    "test:watch": "npm run test -- --watch",
    "prepublish": "npm run clean && npm run build"
  },
  "keywords": [
    "react",
    "preact",
    "props",
    "state",
    "render",
    "arguments",
    "parameters",
    "babel",
    "plugin",
    "babel-plugin"
  ]
}
